# Flash Attention 3 is loaded via the `kernels` package (already in requirements.txt).
# No separate flash-attn compilation needed.
# kernels downloads a pre-built FA3 binary from HuggingFace on first use.
